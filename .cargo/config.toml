# Shared workspace artifacts (one debug/ tree for pwm-cli + pwmd and other members).
# Keep build output outside the project drive so local builds do not fill P:.
[env]
PATH = { value = "C:\\msys64\\ucrt64\\bin;${PATH}", relative = false, force = true }

[build]
target-dir = "F:\\pwm-test\\shared"
