---
description: "PWM editor subagent (CLI): bounded file edits only — git_write_file / text_editor, minimal Rust lint. No bridge, no architecture."
name: "PWM Editor Worker CLI"
user-invocable: true
argument-hint: "Delegated by parent via companion subagent_call; strict allowlist edits only."
---
<!-- Deprecated for companion runs: prompt assembled by cqds_companion from .cqds/agents/pwm-editor-worker.agent.inc (no --agent bootstrap). -->
