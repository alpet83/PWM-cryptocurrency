{
  "schema_version": 1,
  "id": "20260616-pwm-cli-bruteforce-max-try-attempt-count",
  "title": "addr-bruteforce: --max-try = число попыток (не ceiling index)",
  "status": "done",
  "brief": "Семантика --max-try: пользователь ожидает количество проверяемых derivation с resume_start, а код использовал абсолютный верхний индекс (start..=max_try). Исправить контракт, тесты, docs/runbook.",
  "acceptance_criteria": [
    "bruteforce.rs: цикл brute по max_try попыткам от start_index (end = start + max_try - 1, saturating u32)",
    "bruteforce_resume_index: убрать/заменить проверку start > max_try (невалидна для attempt-count)",
    "cmd_addr.rs: attempts_budget = max_try; stderr plan: start_index, attempts=max_try, end_index inclusive",
    "bf_no_match_msg и тесты обновлены под новую семантику",
    "addr-derive: та же семантика max_try если общий bruteforce core",
    "docs/pwm-cli.md + v6-owner-stability-soak-50k.md: max-try = attempt count",
    "cargo test -p pwm-cli — green (CARGO_TARGET_DIR=F:/pwm-test/pwm-protocol)"
  ],
  "depends_on": ["20260616-pwm-cli-bruteforce-no-match-graceful"],
  "delegations": [
    {
      "agent": "pwm-coding",
      "prompt_summary": "max-try attempt-count semantics + tests + docs",
      "result": "PASS",
      "artifacts": ["crates/pwm-cli/src/cmd_addr.rs", "crates/pwm-cli/src/tests/mod.rs", "docs/pwm-cli.md"],
      "tokens": { "source": "estimate", "total": 21000, "confidence": "low" },
      "done_at": "2026-06-16",
      "via": "cursor-task-not-bridge",
      "note": "share_ticket failed (no project_id); owner exception — coding in Cursor"
    }
  ],
  "notes": "Renamed from 20260617-* (wrong creation-date prefix). Bridge share_ticket не выполнен; слайс закрыт через Cursor pwm-coding по явной просьбе владельца."
}
